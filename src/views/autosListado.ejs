<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Autos</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/styles.css" rel="stylesheet">
</head>

<body>
    <%- include('partials/header') -%>
        <div class="container mt-5">
            <h1 class="mb-4 text-center">Listado de Autos</h1>
            <div class="row">
                <% autos.forEach(auto=> { %>
                    <div class="col-md-3">
                        <div class="card mb-4 bg-custom text-white shadow-sm border-0 rounded">
                            <div class="card-body text-center d-flex flex-column justify-content-between">
                                <div>
                                    <h5 class="card-title text-center fw-bold fs-4 mb-4">
                                        <%= auto.marca %>
                                            <%= auto.modelo %>
                                    </h5>
                                    <img src="<%= auto.path_foto %>" class="card-img-top img-fluid mb-4"
                                        alt="Foto del auto" style="max-width: 100%; height: 200px; object-fit: cover;">
                                </div>
                                <div class="card-text">
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-calendar me-2"></i>
                                        <span class="fw-bold fst-italic">Año:</span>
                                        <span class="ms-2">
                                            <%= auto.anio %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-palette me-2"></i>
                                        <span class="fw-bold fst-italic">Color:</span>
                                        <span class="ms-2">
                                            <%= auto.color %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-fuel-pump me-2"></i>
                                        <span class="fw-bold fst-italic">Tipo de Combustible:</span>
                                        <span class="ms-2">
                                            <%= auto.tipo_combustible %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-speedometer2 me-2"></i>
                                        <span class="fw-bold fst-italic">Kilometraje:</span>
                                        <span class="ms-2">
                                            <%= auto.kilometraje %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-gear me-2"></i>
                                        <span class="fw-bold fst-italic">Transmisión:</span>
                                        <span class="ms-2">
                                            <%= auto.transmision %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-currency-dollar me-2"></i>
                                        <span class="fw-bold fst-italic">Precio:</span>
                                        <span class="ms-2">$<%= auto.precio %></span>
                                    </p>
                                    <p class="d-flex align-items-center mb-2">
                                        <i class="bi bi-door-closed me-2"></i>
                                        <span class="fw-bold fst-italic">Puertas:</span>
                                        <span class="ms-2">
                                            <%= auto.puertas %>
                                        </span>
                                    </p>
                                    <p class="d-flex align-items-center mb-4">
                                        <i class="bi bi-gear-wide me-2"></i>
                                        <span class="fw-bold fst-italic">Tipo de Motor:</span>
                                        <span class="ms-2">
                                            <%= auto.tipo_motor %>
                                        </span>
                                    </p>
                                </div>
                                <div class="mt-auto">
                                    <a href="/autos/modificar/<%= auto.id %>" class="btn btn-primary me-5">Modificar</a>
                                    <form action="/autos/eliminar/<%= auto.id %>?_method=DELETE" method="POST"
                                        class="d-inline">
                                        <button type="button" onclick="confirmarEliminarAuto('<%= auto.id %>')"
                                            class="btn btn-danger">Eliminar</button>
                                    </form>

                                </div>
                            </div>
                        </div>
                    </div>
                    <% }); %>
            </div>
        </div>
        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"></script>
        <!-- CDN para SweetAlert2 -->
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
        <script src="/js/sweetAlert.js"></script>

</body>
<%- include('partials/footer') %>

</html>